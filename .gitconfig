[init]
	defaultBranch = main

# Machine-local settings — identity, work email, credential helpers, etc.
# Not tracked by this repo; create it per machine. See README.
[include]
	path = ~/.gitconfig.local
